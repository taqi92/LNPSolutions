<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/view_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.eis.lnp.lnpsolutions.LoginActivity" >

    <EditText
        android:id="@+id/user_pass"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/user_user"
        android:layout_alignStart="@+id/user_user"
        android:layout_below="@+id/user_user"
        android:layout_marginTop="0dp"
        android:ems="10"
        android:hint="@string/edit_message_pass"
        android:inputType="textPassword" />

    <EditText
        android:id="@+id/user_user"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="0dp"
        android:layout_marginLeft="0dp"
        android:layout_marginTop="50dp"
        android:hint="@string/edit_message_user"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/loginbtn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignLeft="@+id/user_pass"
        android:layout_alignStart="@+id/user_pass"
        android:layout_below="@+id/user_pass"
        android:onClick="login"
        android:text="@string/login_button"
        android:background="@drawable/gloss_button"/>

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/imageView"
        android:background="@drawable/ic_launcher"
        android:layout_below="@+id/loginbtn" />

</RelativeLayout>
